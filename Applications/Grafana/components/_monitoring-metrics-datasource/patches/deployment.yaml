---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: grafana
spec:
  template:
    spec:
      containers:
        - name: grafana
          env:
            - name: VM_USERNAME
              valueFrom:
                secretKeyRef:
                  name: grafana-monitoring-datasource
                  key: username
            - name: VM_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: grafana-monitoring-datasource
                  key: password
          volumeMounts:
            - name: monitoring-metrics-datasource
              mountPath: /etc/grafana/provisioning/datasources/monitoring-metrics.yaml
              subPath: monitoring-metrics.yaml
              readOnly: true
      volumes:
        - name: monitoring-metrics-datasource
          configMap:
            name: grafana-monitoring-metrics-datasource
