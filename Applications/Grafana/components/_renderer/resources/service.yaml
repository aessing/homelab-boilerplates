---
apiVersion: v1
kind: Service
metadata:
  name: grafana-oss-renderer
  labels:
    app.kubernetes.io/component: 'renderer'
spec:
  selector:
    app.kubernetes.io/component: 'renderer'
  clusterIP: None
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 8081
      name: http
  type: ClusterIP
