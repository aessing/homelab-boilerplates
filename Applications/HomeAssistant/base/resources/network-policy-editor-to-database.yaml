---
apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: editor-to-database
spec:
  podSelector:
    matchLabels:
      cnpg.io/cluster: homeassistant-db
  policyTypes:
    - Ingress
  ingress:
    - from:
        - namespaceSelector:
            matchLabels:
              kubernetes.io/metadata.name: homeassistant
        - podSelector:
            matchLabels:
              app.kubernetes.io/component: editor
              app.kubernetes.io/name: homeassistant
      ports:
        - protocol: TCP
          port: 5432
