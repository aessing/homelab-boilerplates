apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: homepage-public-custom
  labels:
    app.kubernetes.io/component: 'application'
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: 0Gi
  storageClassName: longhorn-retain
