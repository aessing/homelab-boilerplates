apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: homepage

buildMetadata:
  - managedByLabel
  - originAnnotations
  #- transformerAnnotations

labels:
  - includeSelectors: false
    pairs:
      app.kubernetes.io/version: 1.3.2 # The current version of the application

resources:
  - ../../base

generators:
  - ./generators/config.yaml

patches:
  - path: ./patches/certificate.json
    target:
      group: cert-manager.io
      version: v1
      kind: Certificate
  - path: ./patches/deployment-env.json # See gethomepage.dev/installation/#homepage_allowed_hosts
    target:
      group: apps
      version: v1
      kind: Deployment
  - path: ./patches/ingressroute.json
    target:
      group: traefik.io
      version: v1alpha1
      kind: IngressRoute
  - path: ./patches/pvc.json
    target:
      version: v1
      kind: PersistentVolumeClaim
      name: homepage-public
  - path: ./patches/resource-quota.json
    target:
      version: v1
      kind: ResourceQuota

transformers:
  # https://github.com/gethomepage/homepage
  - ./transformers/images.yaml
  - ./transformers/labels.yaml
  - ./transformers/replicas.yaml
