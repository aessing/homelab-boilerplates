---
apiVersion: v1
kind: Service
metadata:
  name: influxdb
  labels:
    app.kubernetes.io/component: 'database'
spec:
  selector:
    app.kubernetes.io/component: 'database'
  clusterIP: None
  ports:
    - protocol: TCP
      port: 8086
      targetPort: 8086
      name: http
  type: ClusterIP
