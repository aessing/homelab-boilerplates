---
apiVersion: v1
kind: Service
metadata:
  name: alloy-metrics
  labels:
    app.kubernetes.io/component: alloy
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/component: alloy
  ports:
    - name: http
      protocol: TCP
      port: 12345
      targetPort: http
