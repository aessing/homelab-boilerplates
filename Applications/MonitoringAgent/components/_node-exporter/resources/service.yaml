---
apiVersion: v1
kind: Service
metadata:
  name: node-exporter
  labels:
    app.kubernetes.io/component: node-exporter
spec:
  type: ClusterIP
  clusterIP: None
  selector:
    app.kubernetes.io/component: node-exporter
  ports:
    - name: metrics
      protocol: TCP
      port: 9100
      targetPort: metrics
