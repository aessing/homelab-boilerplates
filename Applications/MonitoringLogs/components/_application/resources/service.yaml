---
apiVersion: v1
kind: Service
metadata:
  name: loki
  labels:
    app.kubernetes.io/component: application
spec:
  type: ClusterIP
  selector:
    app.kubernetes.io/component: application
  ports:
    - name: http
      protocol: TCP
      port: 3100
      targetPort: http
