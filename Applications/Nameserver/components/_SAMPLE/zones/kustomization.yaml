apiVersion: kustomize.config.k8s.io/v1alpha1
kind: Component

configMapGenerator:
  - name: nameserver-zones
    behavior: create
    files:
      - named.0
      - named.127
      - named.255
      - named.empty
      - named.local
      - named.root
      - db.blocklist
      - db.10.in-addr.arpa
      - db.example.org
