---
apiVersion: builtin
kind: SecretGenerator
metadata:
  name: uptimekuma-database-root
behavior: create
options:
  labels:
    app.kubernetes.io/component: 'database'
  annotations:
    # Reloads handled by kustomize with suffix
    reloader.stakater.com/match: 'false' # Marks the config/secret as eligible for reload in search mode
type: Opaque
envs:
  - ./secrets/secret-uptimekuma-database-root.env
